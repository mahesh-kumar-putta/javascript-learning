// Method chaining : calling one method from another in a single statement
// 

const userName = window.prompt(`Enter your name`);

console.log(userName);

console.log(userName.trim().toUpperCase());

